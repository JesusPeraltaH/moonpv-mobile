import 'package:flutter/material.dart';

class ConteoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Conteo'),
      ),
      body: Center(
        child: Text('Pantalla de Conteo'),
      ),
    );
  }
}
